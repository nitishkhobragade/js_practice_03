////In this example, we will pass an array and check the resultant.

var arr = new Array(1, 2, 34, 4, 5);
document.write(Array.isArray(arr)); //It will return true.

////output- Hence, the output says that the passed value is an array.
