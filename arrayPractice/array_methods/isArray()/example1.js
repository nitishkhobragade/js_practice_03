////This example shows a basic implementation of isArray() method.

document.write(Array.isArray(1, 2, 3, 4)); //Testing the passed values.

////output-It is clear that the passed values were not forming any array. Thus, the result is false.
