// Example 3: Since the argument passed to the function isArray() is a map therefore this function returns false as the answer.

// JavaScript code for isArray() function
function func() {
  console.log(Array.isArray({ Niti: 123 }));
}
func();
